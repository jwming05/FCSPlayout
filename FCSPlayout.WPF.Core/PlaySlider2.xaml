<UserControl x:Class="FCSPlayout.WPF.Core.PlaySlider2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FCSPlayout.WPF.Core"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:SecondsToTimeCode x:Key="SecondsToTimeCode"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding ElementName=slider,Path=Value,Converter={StaticResource SecondsToTimeCode}}"
                   Margin="3" VerticalAlignment="Center"/>
        <Slider Name="slider" Minimum="0" Value="0" Grid.Column="1" ValueChanged="slider_ValueChanged"/>
    </Grid>
</UserControl>
